This is a version of RtN that runs on 64-bit *nix systems.
It is a statically linked binary and should ostensibly be portable.
Note the original `rtn_original` is prior a bugfix (featurefix?) where the `-k` flag was added
AND the `-w` flag was modified to keep all reads.


